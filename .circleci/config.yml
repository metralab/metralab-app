jobs:
  build:
    docker:
      - image: cimg/node:14.10.1
    steps:
      - checkout
      - run: cd /metralab-app
      - run: docker build -t metralab-cicd .
      # - run: flutter build appbundle
      # - run: flutter channel beta
      # - run: flutter upgrade
      # - run: flutter config --enable-web
      # - run: flutter create .
      # - run: flutter build web
